services:
  frontend:
    image: localhost/kanban-frontend
    container_name: kf
    ports:
      - "3000:3000"

  backend:
    image: localhost/kanban-backend
    container_name: kb
    ports:
      - "8080:8080"

  database:
    image: postgres
    container_name: kd
    restart: always
    environment:
      POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
